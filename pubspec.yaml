name: note_canvas_application
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.19.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.0.0
  collection: ^1.17.0
  flutter_bloc: ^8.0.0
  flutter_markdown: ^0.6.0
  freezed_annotation: ^2.2.0
  cupertino_icons: ^1.0.2
  uuid: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.3.0
  freezed: ^2.3.0
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - 'assets/images/'